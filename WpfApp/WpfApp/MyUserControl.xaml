<UserControl x:Class="WpfApp.MyUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="700" MouseEnter="UserControl_MouseEnter" MouseLeave="UserControl_MouseLeave" MouseDown="UserControl_MouseDown">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Rectangle x:Name="ColorRect" Grid.Column="0" Grid.ColumnSpan="2" Height="100" Fill="#eeeeee"/>

        <Image x:Name="ImageView" Grid.Column="0"/>
        <Label x:Name="Description" Grid.Column="1" Content="UserControl" VerticalAlignment="Center" FontSize="20" Margin="10 10 10 10"/>
    </Grid>
</UserControl>
